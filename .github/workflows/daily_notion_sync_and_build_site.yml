name: Daily Notion Sync

on:
  schedule:
    - cron: "0 13 * * *"  # 台灣時間21:00 (UTC+8)
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  run-sync:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run daily_push_check.py
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_PAGE_ID: ${{ secrets.NOTION_PAGE_ID }}
        run: python ./scripts/daily_push_check.py

      - name: Build site
        run: python ./scripts/build_site.py

      - name: Check site folder
        run: |
          echo "📁 列出 site 資料夾內容："
          ls -R ./site || echo "⚠️ 沒找到 site！"

      - name: Commit and push updated markdown
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git remote set-url origin https://x-access-token:${GH_PAT}@github.com/$GITHUB_REPOSITORY

          git add .
          git commit -m "📥 Update daily log for $(date +'%Y-%m-%d')" || echo "No changes to commit"
          git push origin main
